import { MustMatch.Validator } from './must-match.validator';

describe('MustMatch.Validator', () => {
  it('should create an instance', () => {
    expect(new MustMatch.Validator()).toBeTruthy();
  });
});
